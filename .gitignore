# Secrets / local config
.env
*.key
*.pem
*.p12

# Runtime artifacts (jobs, sqlite, outputs) — NEVER commit, may contain PII
_work/
*.db
*.sqlite

# Models (large; distribute separately)
models/*.onnx
models/*.onnx.aes
models/*.pt

# Datasets (may contain real documents = PII)
datasets/
*.zip

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
*.egg-info/

# OS / IDE
.DS_Store
.idea/
.vscode/

# Build artifacts
build/
dist/
*.spec
*.log
_build_*
